# pgbridge central config — connection string to pansoinco_suite only.
# All database-to-module mappings are read from there (sw_pgb table).
#
# Usage:
#   pgbridge [/path/to/central.conf]       # explicit path
#   PGBRIDGE_CENTRAL_CONFIG=/path pgbridge  # env var
#   pgbridge                               # defaults to /etc/pgbridge/central.conf

postgres://tyutyu:801031@localhost:5432/pansoinco_suite

